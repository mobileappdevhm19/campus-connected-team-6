import 'package:flutter/material.dart';

class TestHelper {
  static buildPage(page) {
    return MaterialApp(
      home: page,
    );
  }

  static buildWidget(page) {
    return MaterialApp(
      home: page,
    );
  }
}
